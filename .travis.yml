language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t salamatburj/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true salamatburj/docker-react npm run test -- --coverage
